# Copy to terraform.tfvars and set your values (do not commit terraform.tfvars).
# Set TAILSCALE_API_KEY and TAILSCALE_TAILNET in the environment for provider auth.

# Optional: override the member email
# member_login_name = "alice@example.com"
